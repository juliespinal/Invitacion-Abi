/**
 * Google Apps Script — backend de confirmaciones (Sheets).
 * Placeholder de estructura (Etapa 2). Implementación completa en la Etapa 7:
 * doPost, validación, lookup de invitado por código, escritura en "Confirmados",
 * generación de ID con LockService, respuesta JSON.
 *
 * Ver CLAUDE.md §23-27 para el detalle de columnas y comportamiento esperado.
 */

function doPost(e) {
  // TODO (Etapa 7)
}
